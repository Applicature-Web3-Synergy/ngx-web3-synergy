import { ApplicatureCustomizeDialogDirective } from './applicature-customize-dialog.directive';

xdescribe('CustomizeDialogDirective', () => {
  xit('should create an instance', () => {
    // const directive = new ApplicatureCustomizeDialogDirective();
    // expect(directive).toBeTruthy();
  });
});
