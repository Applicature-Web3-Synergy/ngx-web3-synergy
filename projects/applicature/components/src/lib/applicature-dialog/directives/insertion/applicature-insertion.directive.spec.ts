import { ApplicatureInsertionDirective } from './applicature-insertion.directive';

xdescribe('ApplicatureInsertionDirective', () => {
  it('should create an instance', () => {
    // const directive = new ApplicatureInsertionDirective();
    // expect(directive).toBeTruthy();
  });
});
