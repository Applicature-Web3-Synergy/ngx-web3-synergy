import { ApplicatureCustomClassDirective } from './applicature-custom-class.directive';

describe('ApplicatureCustomClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplicatureCustomClassDirective();
    expect(directive).toBeTruthy();
  });
});
