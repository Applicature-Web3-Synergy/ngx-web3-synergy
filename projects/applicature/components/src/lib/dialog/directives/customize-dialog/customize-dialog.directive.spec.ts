import { CustomizeDialogDirective } from './customize-dialog.directive';

describe('CustomizeDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomizeDialogDirective();
    expect(directive).toBeTruthy();
  });
});
