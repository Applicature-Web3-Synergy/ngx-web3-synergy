import { InsertionDirective } from './insertion.directive';

xdescribe('InsertionDirective', () => {
  it('should create an instance', () => {
    // const directive = new InsertionDirective();
    // expect(directive).toBeTruthy();
  });
});
