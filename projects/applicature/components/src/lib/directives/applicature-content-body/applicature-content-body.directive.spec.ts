import { ApplicatureContentBodyDirective } from './applicature-content-body.directive';

describe('ApplicatureContentBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplicatureContentBodyDirective();
    expect(directive).toBeTruthy();
  });
});
