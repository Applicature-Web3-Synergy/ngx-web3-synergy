import { ApplicatureTriggerDirective } from './applicature-trigger.directive';

describe('ApplicatureTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplicatureTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
