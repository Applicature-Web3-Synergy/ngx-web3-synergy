import { AucCustomClassDirective } from './custom-class.directive';

describe('AucCustomClassDirective', () => {
  it('should create an instance', () => {
    // const directive = new AucCustomClassDirective();
    // expect(directive).toBeTruthy();
  });
});
